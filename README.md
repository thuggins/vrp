# Vehicle Routing Problem

## How To Run

`python evaluateShared.py --cmd "python vrp.py" --problemDir trainingProblems`

## Docker

- Build `docker build -t vrp .`
- Run `docker run --rm -it vrp /bin/bash`
